<p>タスク 編集</p>
<%= form_with model: @task, local: true do |f| %>
  <p>お名前：<%= f.text_field :title %></p>
  <p>年齢：<%= f.text_field :content %></p>
  <%= f.submit "送信" %>
<% end %>
<%= link_to "一覧画面へ", tasks_path %>
